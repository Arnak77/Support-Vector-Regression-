importing all the libraries-:
checking if any value is missing-:
separate dependent and independent variables-:
    1] iloc()
    2] value()
Machine Learning Libraries-:
  1] from sklearn.svm import SVR
  2] use parameter like-:
       SVR(*, kernel="rbf", degree=3, gamma="scale", coef0=0.0, tol=1e-3, C=1.0, epsilon=0.1, shrinking=True, cache_size=200, verbose=False, max_iter=-1)
  3] and predict the salary useing SVR
